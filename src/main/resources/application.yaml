spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    password: "root"
    url: jdbc:mysql://localhost:3306/shelter?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true
    username: "root"
  server:
    port: 8082
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
